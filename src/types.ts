export enum Tea {
  Black = "black",
  Green = "green",
  Red = "red",
}

export enum BlackTeaTimers {
  Light = 4,
  Medium = 4.5,
  Strong = 5,
}

export enum GreenTeaTimers {
  Light = 2,
  Medium = 2.5,
  Strong = 3,
}

export enum RedTeaTimers {
  Light = 5,
  Medium = 7,
  Strong = 8,
}
